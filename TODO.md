# UPI Offline Mesh — Backend TODO

- [x] 1. Analyze all source files, templates, config, tests
- [x] 2. Create project scaffold (`requirements.txt`, folder structure)
- [x] 3. Create Config (`config.py`)
- [x] 4. Create Models (`Account`, `Transaction` — SQLAlchemy ORM with optimistic locking)
- [x] 5. Create Crypto (`ServerKeyHolder`, `HybridCryptoService` — `cryptography` library)
- [x] 6. Create Services (`DemoService`, `IdempotencyService`, `VirtualDevice`, `MeshSimulatorService`, `SettlementService`, `BridgeIngestionService`)
- [x] 7. Create Controllers (`ApiController`, `DashboardController` — Flask routes)
- [x] 8. Create Dashboard template (plain HTML/Jinja2)
- [x] 9. Create bootstrap `app.py` (Flask app, DB init, scheduler)
- [x] 10. Create tests (`test_idempotency` — pytest)
- [x] 11. Validate by running app and tests
- [x] 12. Reorganize Python files at project root


